services:
  backend:
    environment:
      NODE_ENV: production
    ports:
      - '${BACKEND_PORT:-5000}:5000'

  frontend:
    environment:
      NODE_ENV: production
    ports:
      - '${FRONTEND_PORT:-3000}:3000'
